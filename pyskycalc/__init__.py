#Load all from classes when `import pyskycalc` or `from pyskycalc import *` is called
from . import classes
from .classes import *

#Same as before but with utils
from . import utils
from .utils import *
